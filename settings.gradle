pluginManagement {
    plugins {
        id 'org.asciidoctor.jvm.convert' version "${asciidoctorJvmVersion}"
        id 'org.ajoberstar.grgit' version "${grgitVersion}"
    }
}

rootProject.name = 'asciidoctor-example'
